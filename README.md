Hash-Function
=============

A program which will get an input file (*.txt format) than it will build an hash table from words of input file and it will find hash codes of each words using hash code function algorithm. 
